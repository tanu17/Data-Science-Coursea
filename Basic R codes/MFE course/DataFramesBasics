https://query1.finance.yahoo.com/v7/finance/download/TSLA?period1=1528203740&period2=1530795740&interval=1d&events=history&crumb=aFUjSRg.U87

Tesla <- read.csv("C:/Users/User/Downloads/TSLA.csv")
class(Tesla)
//returns "data.frame"
view(Tesla)
min(Tesla[["High"]]
sd(Tesla[["Open"]]
plot(Tesla$Open)
nrow(Tesla)
ncol(Tesla)
colnames(Tesla)       //names(Tesla) is the same
rownames(Tesla)
str(Tesla)
//gives summary of the data

plot(Tesla$Date, Telsa$Close)

Tesla[order(Tesla$Close, decreasing = TRUE),]

save() // save the environment and variables from memory
save(Tesla, file="C/Desktop/1.RData")
